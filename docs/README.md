# truffle-ts documentation

- **[Architecture](architecture.md)** — the parse → resolve → filter → sort →
  explain pipeline, the `Finder` seam, and the pure-core discipline.
- **[Query language](query-language.md)** — what a `find` query understands:
  vendors, processors, GPUs, sizes, vCPU/memory/GPU-count, architecture, network,
  EFA, apps, and qualitative sort words.
- **[Bundled catalog](catalog.md)** — the offline instance snapshot + static
  pricing: what's in it, how it's shaped, and how to regenerate it.
- **[API reference](api.md)** — the public API. The generated
  [TypeDoc reference](https://spore-host.github.io/truffle-ts/api/) is published
  alongside the demo.

For install + a quick start, see the [top-level README](../README.md). For the
live demo, see https://spore-host.github.io/truffle-ts/.
